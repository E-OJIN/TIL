## Github 사용법 -- API key 보안

- **Learned** : Github에 프로젝트를 올릴 때 API Key나 비밀번호, DB 접속 정보 같은 민감 정보를 그대로 노출하면 안됨. 이를 방지하기 위해 환경변수를 분리하고 .gitignore 설정해 보안

- **Key Takeaways** :
  1. 설정파일 분리 : 실제 API Key는 코드에 직접 쓰지 않고 'config.py' 또는 .env 같은 별도 파일로 분리
  2. '.gitignore' 설정: 민감한 설정 파일명을 '.gitignore'에 등록해 github 업로드 시 자동으로 제외시키기
  3. Template 제공 : 파일을 열어보는 사람이 코드를 실행해 볼 수 있도록 'config_template.py' (가짜 키 껍데기)를 대신 업로드

```python
# step 1. API Key를 따로 담을 설정 파일 만들기

## config.py 파일 내용
DART_API_KEY = "여기에_본인의_실제_api_키를_넣으세요"

# step 2. 실제 코드에서는 위의 config.py 불러와 사용

## main.ipynb 또는 analysis.py 파일
import OpenDartReader
from config import DART_API_KEY  # 방금 만든 파일에서 키를 가져옴

dart = OpenDartReader(DART_API_KEY)
## 이후 분석 코드 작성...

# step 3. .gitignore 파일 만들기 (핵심 보안장치)

## .gitignore 파일 내용
config.py
.env
__pycache__/
.ipynb_checkpoints/

# step 4. 코드실행 설명서 Template 만들기

## config_template.py 파일 내용 (이건 깃허브에 올려도 됨!)
DART_API_KEY = "YOUR_API_KEY_HERE"
##README.md 파일에 설명: 실행하려면 config_template.py 파일 복사해 이름 config.py로 변경, 본인의 API Key 입력
```
